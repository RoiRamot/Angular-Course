import { LocliazationPipe } from './locliazation.pipe';

describe('LocliazationPipe', () => {
  it('create an instance', () => {
    const pipe = new LocliazationPipe();
    expect(pipe).toBeTruthy();
  });
});
