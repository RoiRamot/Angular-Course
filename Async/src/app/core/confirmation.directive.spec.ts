import { ConfermationDirective } from './confermation.directive';

describe('ConfermationDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfermationDirective();
    expect(directive).toBeTruthy();
  });
});
